import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Bookings = () => {
  return (
    <View style={{ flex: 1, backgroundColor: "red", padding: 8 }}>
      <Text>Bookings</Text>
    </View>
  );
};

export default Bookings;

const styles = StyleSheet.create({});
